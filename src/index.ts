/**
 * mcp-voice — produce/edit/transform text in a specific human voice.
 *
 * Package entry. Modules are added per spec (ingestion first); see README.
 */

export const NAME = "mcp-voice";
export const VERSION = "0.1.0";
